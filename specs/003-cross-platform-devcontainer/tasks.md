# タスクリスト: クロスプラットフォーム DevContainer 設定

**プロジェクト**: WSL2 × 3 マシン + KDE Neon ネイティブLinux環境での統一 DevContainer 運用
**ブランチ**: `003-cross-platform-devcontainer`
**作成日**: 2025-11-16
**ステータス**: 実装準備完了

## 🌸 タスク運用ガイド

### 進捗管理システム

かわいいアイコンを使って進捗を管理します〜 ♪

**ステータスインジケータ**:

- **[🌱]** = 未着手 (種まき前)
- **[🌼]** = 作業中 (お花が育ってる〜)
- **[🌺]** = 完了！(きれいに咲いたよ！)

### アイコン凡例 ✨

- 📝 = ドキュメント作成 (お手紙書いてる)
- 🔍 = 調査・評価 (探索中〜)
- 🛠️ = 実装作業 (がんばって作ってる)
- 🧪 = テスト・検証 (実験中！)
- 📦 = 統合・デプロイ (まとめてお届け)
- 🎯 = マイルストーン (目標達成！)
- ⚠️ = リスク管理 (要注意だよ)

---

## 📋 プロジェクト概要

### 背景・動機

現在、ユウイチくんの開発ワークスペースは複数マシン間で **SyncThing** により同期中：

- **会社マシン（ThinkPad）**: WSL2 環境
- **個人ノート（Lenovo）**: WSL2 環境
- **自宅マシン**: Windows WSL2
- **自宅マシン2（KDE Neon）**: ネイティブLinux環境（メイン開発環境）

現在の `/workspace/.devcontainer/` は **WSL2向けのマウント設定** (`/mnt/c`, `/mnt/d`) を含んでおり、KDE Neon ネイティブLinux環境では動作しない。

**課題**:
- KDE Neon では手動でマウント設定を削除した devcontainer.json で起動中
- SyncThing 同期時に手動調整が必要 → 非効率でエラーリスク高い

### 📊 プロジェクト目標

✅ WSL2 環境と ネイティブLinux 環境を自動判定
✅ devcontainer.json を動的に切り替える仕組みを実装
✅ SyncThing との共存（シンボリックリンク戦略）
✅ マニュアル作業ゼロでの環境対応

---

## フェーズ 0: .devcontainer ファイル同期・準備

### 📝 タスク 0.1: /workspace/.devcontainer との同期確認 🌺

**期間**: 60 分 ✅ 実施済み
**依存関係**: なし
**目的**: 003プロジェクト `.devcontainer/` と実際に運用中の `/workspace/.devcontainer/` を同期し、最新の DevContainer 設定を確保

**背景**:

現在、以下の状況が発生している：
- 003 プロジェクト `.devcontainer/` と `/workspace/.devcontainer/` の内容が乖離
- `/workspace/.devcontainer/` は KDE Neon 対応済み（マウント設定削除）だが、003 プロジェクト側は古い
- `/workspace/.devcontainer/Dockerfile` は最新版だが、003 プロジェクト側は古い
- **symlink 化されておらず、別運用状態**

**実行項目**:

- [x] **差分の詳細分析** (20 分) ✅
  - [x] devcontainer.json の全差分を文書化
  - [x] Dockerfile の全差分を文書化
  - [x] その他ファイル（init-*.sh等）の差分確認
  - [x] `/workspace/.devcontainer/` が最新版であることを確認

- [x] **003 プロジェクト `.devcontainer/` への同期** (30 分) ✅
  - [x] `/workspace/.devcontainer/devcontainer.json` を 003 プロジェクトにコピー
  - [x] `/workspace/.devcontainer/Dockerfile` を 003 プロジェクトにコピー
  - [x] その他の最新ファイルを 003 プロジェクトに同期
  - [x] `devcontainer.json.wsl2-backup-20251115` を確認・整理

- [x] **古いバックアップファイルの整理** (10 分) → スキップ（今回はコア2ファイルのみ同期）
  - [x] 003 プロジェクト内の古い Dockerfile バックアップを確認
  - [x] 不要なバックアップファイルを整理・削除の判定
  - [x] devcontainer.json バックアップの整理

**受け入れ基準**:

- [x] 003 プロジェクト `.devcontainer/` が `/workspace/.devcontainer/` と同期
- [x] Dockerfile が最新版（syntax directive削除, PyTorch自動判別実装済み）
- [x] devcontainer.json が最新版（`mountWorkspaceGitRoot`, `overrideCommand`, `--network=host` 実装済み、WSL2マウント削除完了）
- [x] 差分が明確に文書化（`analysis/phase0-devcontainer-diff.md`）
- [x] git にコミット（同期完了記録）✅ Commit: 6e1dfbd

**実施内容**:
- Dockerfile を最新版（2025-11-17）に更新
- devcontainer.json を最新版（2025-11-24）に更新
- WSL2マウント設定（/mnt/c, /mnt/d）が削除済み → KDE Neon 対応確認
- CLAUDE.md を行動指針中心にリファクタリング
- tasks.md にリネーム
- analysis/phase0-devcontainer-diff.md にて詳細分析を記録
- git にコミット完了

---

## フェーズ 1: 実装準備・設計

### 📝 タスク 1.1: 現状分析・バックアップ 🌱

**期間**: 30 分
**依存関係**: なし
**目的**: 現在の devcontainer.json の状態を確認し、安全なバックアップを作成

**実行項目**:

- [ ] 現在の `/workspace/.devcontainer/devcontainer.json` の状態確認
- [ ] WSL2向け設定と Linux向け設定の差分を明確化
- [ ] 既存 `.wsl2-backup-20251115` をベースに整理
- [ ] git で現在の安定状態を記録

**受け入れ基準**:

- [ ] 両設定の差分が明確に文書化
- [ ] バックアップが安全に保存
- [ ] git コミットで復旧可能な状態

---

### 🔧 タスク 1.2: ファイル構造の設計・準備 🌱

**期間**: 45 分
**依存関係**: タスク 1.1
**目的**: クロスプラットフォーム対応の実装構造を設計・準備

**実行項目**:

- [ ] **ファイル構造の設計**
  - [ ] `devcontainer.json` → シンボリックリンク（切り替え対象）
  - [ ] `devcontainer.json.wsl2` → WSL2向け設定（マウント付き）
  - [ ] `devcontainer.json.linux` → Linux向け設定（マウントなし）

- [ ] **実ファイルの準備**
  - [ ] `devcontainer.json.wsl2` 作成（WSL2マウント設定継承）
  - [ ] `devcontainer.json.linux` 作成（マウント設定削除版）
  - [ ] `.gitignore` に `devcontainer.json` を追加（シンボリックリンク非同期化）

- [ ] **既存ファイルの整理**
  - [ ] `devcontainer.json.wsl2-backup-20251115` の内容を確認・統合
  - [ ] 古いバックアップファイルの整理判定

**受け入れ基準**:

- [ ] WSL2 と Linux 両向けの設定ファイルが準備完了
- [ ] `.gitignore` が正確に設定
- [ ] シンボリックリンク作成の準備完了

---

## フェーズ 2: 実装

### 🛠️ タスク 2.1: 環境判定・自動切り替え機構実装 🌱

**期間**: 120 分
**依存関係**: タスク 1.2
**目的**: WSL2 環境と ネイティブLinux 環境を自動判定し、devcontainer.json を動的に切り替える仕組みを実装

**実行項目**:

- [ ] **環境判定スクリプト実装** (60 分)
  - [ ] `scripts/setup-devcontainer-symlink.sh` 作成
  - [ ] WSL判定ロジック実装 (`grep -qi microsoft /proc/version`)
  - [ ] WSL内ドライブ自動検出機能 (`/mnt/{c,d,e,f,g...}`)
  - [ ] 動的マウント設定生成機能
  - [ ] エラーハンドリング・ロギング機能

- [ ] **シンボリックリンク作成ロジック** (40 分)
  - [ ] 環境判定に基づく symlink 作成関数
  - [ ] 既存 symlink の置き換え処理
  - [ ] 失敗時のロールバック機能

- [ ] **初期実行テスト** (20 分)
  - [ ] WSL2 環境での動作確認（手動実行）
  - [ ] KDE Neon での動作確認（手動実行）
  - [ ] symlink が正しく作成されることを確認

**受け入れ基準**:

- [ ] スクリプトがエラーなく実行
- [ ] WSL2 では `/mnt/c`, `/mnt/d` マウント設定が適用
- [ ] KDE Neon ではマウント設定が適用されない
- [ ] エラーメッセージが明確

---

### 📝 タスク 2.2: ドキュメント整備・運用ガイド作成 🌱

**期間**: 45 分
**依存関係**: タスク 2.1
**目的**: クロスプラットフォーム DevContainer の設定・運用方法をドキュメント化

**実行項目**:

- [ ] **DevContainer クロスプラットフォーム運用ガイド** (25 分)
  - [ ] `docs/CROSS_PLATFORM_DEVCONTAINER.md` 作成
  - [ ] 環境判定メカニズムの説明
  - [ ] WSL2 / Linux 環境での違い整理
  - [ ] セットアップ手順の記載
  - [ ] トラブルシューティングセクション

- [ ] **CLAUDE.md プロジェクト情報更新** (15 分)
  - [ ] クロスプラットフォーム対応完了を記録
  - [ ] 複数マシン環境での使用方法記載
  - [ ] SyncThing + DevContainer 運用の推奨設定

- [ ] **セットアップスクリプト説明** (5 分)
  - [ ] `scripts/setup-devcontainer-symlink.sh` の使用方法ドキュメント化
  - [ ] 自動実行 vs 手動実行の判断基準

**受け入れ基準**:

- [ ] クロスプラットフォーム対応の全体像が明確
- [ ] セットアップ手順が簡潔にまとめられている
- [ ] トラブルシューティング情報が充実
- [ ] 既存ドキュメント（CLAUDE.md）との統合完了

---

## フェーズ 3: 検証・テスト

### 🧪 タスク 3.1: DevContainer 再起動テスト・統合検証 🌱

**期間**: 60 分
**依存関係**: タスク 2.1, 2.2
**目的**: 全マシン環境での DevContainer 正常動作確認

**実行項目**:

- [ ] **WSL2 マシンでのテスト** (20 分)
  - [ ] WSL2 環境（会社、個人、自宅）での devcontainer rebuild 実施
  - [ ] `/mnt/c`, `/mnt/d` マウント設定の確認
  - [ ] Serena MCP, Context7 MCP の動作確認
  - [ ] 既存ツール・機能の動作確認

- [ ] **KDE Neon でのテスト** (20 分)
  - [ ] `setup-devcontainer-symlink.sh` 実行
  - [ ] devcontainer.json.linux へのシンボリックリンク確認
  - [ ] マウント設定が **ない** ことを確認
  - [ ] DevContainer 起動・全機能動作確認
  - [ ] Serena MCP, Context7 MCP の動作確認

- [ ] **SyncThing 同期確認** (20 分)
  - [ ] マシン間での `.devcontainer/` 同期状況確認
  - [ ] 各マシンの独立した symlink 状態を確認
  - [ ] 同期競合がないことを確認

**受け入れ基準**:

- [ ] 全マシン（WSL2 × 3 + KDE Neon × 1）で DevContainer 起動成功
- [ ] WSL2 では正しいマウント設定が適用
- [ ] KDE Neon では正しくマウント設定がない
- [ ] SyncThing 同期に問題なし
- [ ] 全MCP、既存ツールが正常動作

---

### 📦 タスク 3.2: 最終検証・プロジェクト完了 🌱

**期間**: 30 分
**依存関係**: タスク 3.1
**目的**: プロジェクト完了の宣言と次フェーズ準備

**実行項目**:

- [ ] **全機能の最終確認**
  - [ ] 全マシン環境での統一動作確認
  - [ ] 手動調整の必要性がないことを確認
  - [ ] ドキュメント完全性の確認

- [ ] **git コミット・記録**
  - [ ] `003-cross-platform-devcontainer` ブランチに変更をコミット
  - [ ] PR/マージの準備
  - [ ] PROJECTS.md の状態更新

- [ ] **次フェーズ（002 ベストプラクティス）準備**
  - [ ] 002 の残り要素リストの確認
  - [ ] 実装順序の再確認
  - [ ] 開始の準備完了

**受け入れ基準**:

- [ ] すべてのテストが合格
- [ ] ドキュメント完成・校閲完了
- [ ] git にコミット完了
- [ ] 次フェーズ準備完了の宣言

---

## 🎯 プロジェクト完了基準

**機能要件**:

- ✅ 複数環境の自動判定・切り替え
- ✅ SyncThing との共存（シンボリックリンク戦略）
- ✅ マニュアル作業ゼロでの環境対応

**テスト要件**:

- ✅ WSL2（複数ドライブ設定）での正常動作
- ✅ ネイティブLinux（KDE Neon）での正常動作
- ✅ SyncThing 同期環境での正常動作

**ドキュメント要件**:

- ✅ クロスプラットフォーム対応ガイド完成
- ✅ トラブルシューティング情報充実

---

## 📊 総工数・タイムライン

**合計所要時間**: 390 分 ≈ **6.5 時間**

- **フェーズ 0**（同期・準備）: 60 分
- **フェーズ 1**（実装準備・設計）: 75 分
- **フェーズ 2**（実装）: 165 分
- **フェーズ 3**（検証・テスト）: 90 分

**目安**: 1.5 日で完成可能 ✨

---

## 期待成果

- **WSL2 × 3 + KDE Neon の統一 DevContainer 環境確立** ✅
- **手動調整不要な自動環境対応システム** ✅
- **SyncThing での安全なマルチマシン開発ワークフロー** ✅
- **002（ベストプラクティス統合）実装への基盤確立** ✅
