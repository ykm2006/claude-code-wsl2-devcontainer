# 実装計画: マルチプロジェクト・マルチマシン開発環境向けDevContainer最適化

> **注意**: これは参照用の日本語版です。正式な実装計画は英語版 (`plan.md`) を参照してください。
> **Note**: This is a Japanese reference version. The official implementation plan is the English version (`plan.md`).

**ブランチ**: `001-optimize-the-devcontainer` | **日付**: 2025-09-13 | **仕様**: [spec.md](./spec.md)
**入力**: `/specs/001-optimize-the-devcontainer/spec.md` からの機能仕様

## 実行フロー (/planコマンドスコープ)
```
1. 入力パスから機能仕様を読み込み
   → 見つからない場合: ERROR "No feature spec at {path}"
2. 技術コンテキストを記入 (NEEDS CLARIFICATIONをスキャン)
   → コンテキストからプロジェクトタイプを検出 (web=frontend+backend, mobile=app+api)
   → プロジェクトタイプに基づいて構造決定を設定
3. 下記の憲法チェックセクションを評価
   → 違反が存在する場合: 複雑性追跡に文書化
   → 正当化不可能な場合: ERROR "Simplify approach first"
   → 進捗追跡を更新: 初期憲法チェック
4. フェーズ0を実行 → research.md
   → NEEDS CLARIFICATIONが残る場合: ERROR "Resolve unknowns"
5. フェーズ1を実行 → contracts, data-model.md, quickstart.md, エージェント固有テンプレートファイル (例：Claude Code用`CLAUDE.md`、GitHub Copilot用`.github/copilot-instructions.md`、Gemini CLI用`GEMINI.md`)
6. 憲法チェックセクションを再評価
   → 新しい違反がある場合: 設計をリファクタリング、フェーズ1に戻る
   → 進捗追跡を更新: 設計後憲法チェック
7. フェーズ2を計画 → タスク生成アプローチを記述 (tasks.mdは作成しない)
8. 停止 - /tasksコマンドの準備完了
```

**重要**: /planコマンドはステップ7で停止します。フェーズ2-4は他のコマンドで実行されます：
- フェーズ2: /tasksコマンドがtasks.mdを作成
- フェーズ3-4: 実装実行 (手動またはツール経由)

## サマリー
既存のDevContainer設定 (../.devcontainer/) を最適化し、一貫性、パフォーマンス、保守性を維持しながら、複数のWindows WSL2マシン間でのマルチプロジェクトワークスペース管理をサポートします。ソリューションは、シームレスなClaude Code統合、WSL2特化最適化、およびチームコラボレーション向けのモジュラー設定を提供します。

## 技術コンテキスト
**言語/バージョン**: マルチ言語サポート (必要に応じてNode.js 20、Python、Go、Rust)
**主要依存関係**: Docker、VS Code DevContainers、Powerlevel10k、Claude Code、git-delta、zsh
**ストレージ**: 永続データ用ボリュームマウント、設定ファイル用バインドマウント
**テスト**: DevContainer検証、クロスプラットフォーム互換性テスト
**ターゲットプラットフォーム**: Windows WSL2ホストでのLinuxコンテナ
**プロジェクトタイプ**: インフラストラクチャ設定 (DevContainer最適化)
**パフォーマンス目標**: 高速なコンテナ起動、最小限のリソースオーバーヘッド、効率的なレイヤーキャッシング
**制約**: クロスプラットフォーム互換性、バージョン管理フレンドリー、マシン固有の秘密なし
**スケール/スコープ**: 複数プロジェクトの同時サポート、3台以上のマシン同期、チームコラボレーション

**ユーザー提供の実装詳細**:
- 現在のDevContainer構造分析: 既存のdevcontainer.json、Dockerfile、init-firewall.sh、.p10k.zshのレビュー
- マルチプロジェクトアーキテクチャ: Dockerレイヤー最適化、ボリュームマウント戦略、リソース分離
- WSL2最適化: Windows WSL2固有のパフォーマンスチューニングとパス処理
- パフォーマンス最適化: コンテナ起動時間、イメージサイズ、ワークフロー効率
- 保守性構造: モジュラー設定、ドキュメント、バージョン管理

## 憲法チェック
*ゲート: フェーズ0リサーチ前に通過する必要があります。フェーズ1設計後に再チェック。*

**簡潔性**:
- プロジェクト数: 1 (最大3 - インフラストラクチャプロジェクト)
- フレームワークを直接使用？ (ラッパークラスなし) - はい、Dockerを直接使用
- 単一データモデル？ (シリアル化が異なる場合を除きDTOなし) - はい、設定スキーマ
- パターンを避けている？ (証明された必要性なしにRepository/UoWなし) - N/A、アプリケーションロジックなし

**アーキテクチャ**:
- すべての機能をライブラリとして？ (直接アプリコードなし) - N/A、インフラストラクチャ設定
- ライブラリ一覧: 設定テンプレート + 検証スキーマ
- ライブラリごとのCLI: 設定生成、検証、テストコマンド
- ライブラリドキュメント: llms.txt形式を計画済み？ - はい

**テスト (妥協不可)**:
- RED-GREEN-リファクタサイクルを強制？ - はい、設定検証テスト
- Gitコミットがテストを実装前に表示？ - はい
- 順序: Contract→Integration→E2E→Unit厳密に従う？ - はい、インフラストラクチャ向けに調整
- 実際の依存関係を使用？ (モックではなく実際のコンテナ) - はい
- 統合テスト対象: 新しい設定、クロスプラットフォーム、パフォーマンス - はい
- 禁止事項: テスト前の実装、REDフェーズのスキップ - 遵守

**可観測性**:
- 構造化ログを含む？ - はい、設定生成とテストログ
- フロントエンドログ → バックエンド？ (統合ストリーム) - N/A
- エラーコンテキストは十分？ - はい

**バージョニング**:
- バージョン番号を割り当て？ (MAJOR.MINOR.BUILD) - はい、1.0.0
- すべての変更でBUILDを増分？ - はい
- 破壊的変更を処理？ (並列テスト、移行計画) - はい

## プロジェクト構造

### ドキュメント (この機能)
```
specs/001-optimize-the-devcontainer/
├── plan.md              # このファイル (/planコマンド出力)
├── research.md          # フェーズ0出力 (/planコマンド)
├── data-model.md        # フェーズ1出力 (/planコマンド)
├── quickstart.md        # フェーズ1出力 (/planコマンド)
├── contracts/           # フェーズ1出力 (/planコマンド)
└── tasks.md             # フェーズ2出力 (/tasksコマンド - /planでは作成されない)
```

### ソースコード (リポジトリルート)
**構造決定**: カスタムDevContainer設定構造 - 従来のアプリケーションではなくインフラストラクチャ最適化

## フェーズ0: アウトラインと調査

1. **上記の技術コンテキストから不明点を抽出**:
   - 各NEEDS CLARIFICATION → 調査タスク
   - 各依存関係 → ベストプラクティスタスク
   - 各統合 → パターンタスク

2. **調査エージェントの生成と派遣**:
   ```
   技術コンテキストの各不明点について:
     タスク: "{機能コンテキスト}のための{不明点}を調査"
   各技術選択について:
     タスク: "{ドメイン}での{技術}のベストプラクティスを見つける"
   ```

3. **発見の統合** を `research.md` で以下の形式を使用:
   - 決定: [選択されたもの]
   - 理由: [選択された理由]
   - 検討された代替案: [評価された他の選択肢]

**出力**: すべてのNEEDS CLARIFICATIONが解決されたresearch.md

## フェーズ1: 設計と契約
*前提条件: research.md完成*

1. **機能仕様からエンティティを抽出** → `data-model.md`:
   - エンティティ名、フィールド、関係
   - 要件からの検証ルール
   - 該当する場合は状態遷移

2. **機能要件からAPI契約を生成**:
   - 各ユーザーアクション → エンドポイント
   - 標準REST/GraphQLパターンを使用
   - OpenAPI/GraphQLスキーマを `/contracts/` に出力

3. **契約から契約テストを生成**:
   - エンドポイントごとに1つのテストファイル
   - リクエスト/レスポンススキーマをアサート
   - テストは失敗する必要がある (まだ実装なし)

4. **ユーザーストーリーからテストシナリオを抽出**:
   - 各ストーリー → 統合テストシナリオ
   - クイックスタートテスト = ストーリー検証ステップ

5. **エージェントファイルを段階的に更新** (O(1)操作):
   - AI アシスタント用に `/scripts/bash/update-agent-context.sh claude` を実行
   - 存在する場合: 現在の計画から新しい技術のみを追加
   - マーカー間の手動追加を保持
   - 最近の変更を更新 (最後の3つを保持)
   - トークン効率のため150行未満を維持
   - リポジトリルートに出力

**出力**: data-model.md、/contracts/*、失敗するテスト、quickstart.md、エージェント固有ファイル

## フェーズ2: タスク計画アプローチ
*このセクションは/tasksコマンドが行うことを説明 - /plan中には実行しない*

**タスク生成戦略**:
- `/templates/tasks-template.md` をベースとして読み込み
- フェーズ1設計ドキュメント (contracts、data model、quickstart) からタスクを生成
- インフラストラクチャタスク: Docker最適化、設定テンプレート化
- 検証タスク: クロスプラットフォームテスト、パフォーマンスベンチマーク
- 統合タスク: Claude Code設定、MCPサーバー設定
- ドキュメントタスク: チームオンボーディングガイド、トラブルシューティングドキュメント

**順序戦略**:
1. **基盤** (インフラストラクチャ設定): Dockerfile最適化、ベース設定
2. **設定** (モジュラーシステム): テンプレートエンジン、検証スキーマ
3. **統合** (プラットフォーム固有): クロスプラットフォーム互換性、パフォーマンス調整
4. **検証** (テストとドキュメント): クイックスタート検証、チームロールアウト
5. タスクが独立している場合は [P] で並列実行をマーク

**期待されるタスクカテゴリ**:
- Dockerレイヤー最適化タスク (3-4タスク)
- 設定システムタスク (4-5タスク)
- クロスプラットフォーム互換性タスク (2-3タスク)
- Claude Code統合タスク (3-4タスク)
- 検証とドキュメントタスク (3-4タスク)
- パフォーマンス最適化タスク (2-3タスク)

**推定出力**: 従来のアプリケーション開発ではなくインフラストラクチャ最適化に焦点を当てた18-25の番号付き、順序付きタスクをtasks.mdに

**標準プロジェクトとの主要な違い**:
- アプリケーションコードではなくインフラストラクチャ重視
- API開発ではなく設定テンプレート化
- 単体テストではなくパフォーマンス検証
- ビジネスロジックテストではなくクロスプラットフォーム互換性テスト

**重要**: このフェーズは/tasksコマンドで実行され、/planでは実行されません

## フェーズ3+: 将来の実装
*これらのフェーズは/planコマンドのスコープ外です*

**フェーズ3**: タスク実行 (/tasksコマンドがtasks.mdを作成)
**フェーズ4**: 実装 (憲法原則に従ってtasks.mdを実行)
**フェーズ5**: 検証 (テスト実行、quickstart.md実行、パフォーマンス検証)

## 複雑性追跡
*憲法チェックで正当化が必要な違反がある場合のみ記入*

| 違反 | なぜ必要か | より簡単な代替案が拒否された理由 |
|-----------|------------|-------------------------------------|
| (例：4番目のプロジェクト) | (現在のニーズ) | (3プロジェクトでは不十分な理由) |
| (例：Repositoryパターン) | (特定の問題) | (直接DB アクセスでは不十分な理由) |

## 進捗追跡
*この チェックリストは実行フロー中に更新されます*

**フェーズステータス**:
- [x] フェーズ0: リサーチ完了 (/planコマンド) - research.md生成
- [x] フェーズ1: 設計完了 (/planコマンド) - data-model.md、contracts/、quickstart.md、CLAUDE.md生成
- [x] フェーズ2: タスク計画完了 (/planコマンド - アプローチのみ記述)
- [ ] フェーズ3: タスク生成 (/tasksコマンド)
- [ ] フェーズ4: 実装完了
- [ ] フェーズ5: 検証通過

**ゲートステータス**:
- [x] 初期憲法チェック: 合格 - インフラストラクチャプロジェクト、モジュラーアプローチ
- [x] 設計後憲法チェック: 合格 - テンプレートベース、パフォーマンス重視の設計
- [x] すべてのNEEDS CLARIFICATION解決 - 技術コンテキストが完全に指定
- [x] 複雑性逸脱の文書化 - インフラストラクチャ最適化では不要

---
*憲法v2.1.1に基づく - `/memory/constitution.md`を参照*